#include "../etoh.h"

/**
 * verdef64betoh - convert 64-bit big-endian ver defs to host byte order
 *
 * @verdef: pointer to 64-bit big-endian ver defs
 */
void verdef64betoh(Elf64_Verdef *verdef)
{
	if (verdef)
	{
	}
}

